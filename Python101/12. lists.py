#list is an array. use list is keyword so use #lst
#lists keep order but are mutable, unlike tuples
lst = ["string", 1, 3.14, ["new item"], "mee"]
print(lst)

for item in lst:
    print("item is: ", item)

